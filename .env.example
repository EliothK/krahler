# Copy this file to .env and fill in real values.
# Vite only exposes variables prefixed with VITE_ to client code.

VITE_GITHUB_USER=""
VITE_CONTACT_EMAIL=""
VITE_ACTIVITY_LIMIT=5

# Used by the prebuild script (src/scripts/fetch-activity.mjs), not by Vite/client code.
GITHUB_USER=""
ACTIVITY_LIMIT=5
GITHUB_TOKEN=
